import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { Create{{pascalCase name}}Schema, type Create{{pascalCase name}}Input } from "@/features/{{camelCase name}}";

interface {{pascalCase name}}FormProps {
    onSubmit: (data: Create{{pascalCase name}}Input) => void;
    defaultValues?: Partial<Create{{pascalCase name}}Input>;
}

export const {{pascalCase name}}Form = ({ onSubmit, defaultValues }: {{pascalCase name}}FormProps) => {
    const {
        register,
        handleSubmit,
        formState: { errors, isSubmitting }
    } = useForm<Create{{pascalCase name}}Input>({
        resolver: zodResolver(Create{{pascalCase name}}Schema),
        defaultValues
    });

    return (
        <form onSubmit={handleSubmit(onSubmit)} className="space-y-4 max-w-md">
            {{#each fields}}
            <div>
                <label htmlFor="{{camelCase this}}" className="block text-sm font-medium mb-1">
                    {{label this}}
                </label>
                <input
                    id="{{camelCase this}}"
                    type="{{inputType this}}"
                    {...register("{{camelCase this}}")}
                    className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                />
                {errors.{{camelCase this}} && (
                    <p className="text-red-600 text-sm mt-1">{errors.{{camelCase this}}?.message}</p>
                )}
            </div>
            {{/each}}
            <button
                type="submit"
                disabled={isSubmitting}
                className="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed"
            >
                {isSubmitting ? "Submitting..." : "Submit"}
            </button>
        </form>
    );
};
