import type { ThemeConfig } from "antd";

// Brand theme tokens. Edit here to retheme every AntD component at once.
export const antdTheme: ThemeConfig = {
  token: {
    colorPrimary: "#171717", // Tailwind neutral-900 — monochrome brand
    borderRadius: 4,
  },
};
